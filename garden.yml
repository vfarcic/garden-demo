kind: Module
type: container
name: silly-demo
services:
- name: backend
  replicas: ${var.service-replicas}
  ports:
  - name: http
    containerPort: 8080
    servicePort: 8080
  healthCheck:  
    httpGet:
      path: /
      port: http
